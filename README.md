# docker-ansible

```sh
export DOCKER_BUILDKIT=1
docker image build -t docker.pkg.github.com/eagleusb/docker-ansible/ansible:latest .
docker image push docker.pkg.github.com/eagleusb/docker-ansible/ansible:latest
```
